How to run the algorithms
- Navigate to the directory: /src/main/java/org/example
- Open one of the runner classes: UGenMaxRunner.java or UHashMaxRunner.java
- Configure the parameters then execute the selected class to start the algorithm
- Available datasets are located in the /datasets directory 
- The execution results will be saved in the /results directory